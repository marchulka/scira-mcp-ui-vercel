# Scira MCP UI for Vercel

Запусти MCP UI как Web-приложение на Vercel. Включает одну кнопку `📦 terraform_snapshot`.

- `/` — главный экран
- `/api/run?tool=terraform_snapshot` — API вызов